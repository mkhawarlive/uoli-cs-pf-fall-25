/*
the following statement can bypass the validation:

s1.marks = 500;

Complete example:

s1.updateMarks(85); // Validation is performed
s1.marks = 500;     // Validation is bypassed

This means the data is not protected.

*/